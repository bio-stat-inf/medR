---
title: "Журнал заданий R"
output:
  html_document:
    toc: true
    toc_float: true
    collapsed: false
    number_sections: false
    toc_depth: 1
    code_folding: hide
editor_options: 
  markdown: 
    wrap: 72
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(message=FALSE,warning=FALSE, cache=TRUE)
```

ВАЖНО: Вы можете удалить все здесь и начать заново. Возможно, вы захотите начать с того, что не будете удалять ничего выше этой строки, пока не узнаете, что этот код делает.

Это файл .RMD. Это обычный текст со специальными функциями. Каждый раз, когда вы пишете просто так, он будет скомпилирован в обычный текст на веб-сайте. Если вы поместите \# перед своим текстом, он создаст заголовок верхнего уровня.

# Мой первый пост

2018 | 7 | 23 Last compiled: `r Sys.Date()`

Обратите внимание, что все, что вы определяете как заголовок верхнего уровня, автоматически помещается в панель содержания слева.

## Заголовок второго уровня

Вы можете добавить больше заголовков, добавив больше хэштегов. Они не будут помещены в оглавление

### заголовок третьего уровня

Вот заголовок еще более низкого уровня

# Мой второй пост (обратите внимание на порядок)

2018 | 7 | 23 Last compiled: `r Sys.Date()`


Я пишу этот урок сверху вниз. И вот как это будет напечатано. Итак, обратите внимание, что второй пост является вторым в списке. Если вы хотите, чтобы ваш последний пост был вверху, создайте новый пост, начиная сверху. Если вы хотите, чтобы сначала был самый старый, сделайте это, а затем продолжайте добавлять в конец

# Добавление текста в таблицу содержания

Этот текст добавлен 

```{r}
samples <- rnorm(100, mean=0, sd=1)
hist(samples)
```

# Добавление материала R

Пока что это просто блог, где вы можете писать простым текстом и размещать свои записи на веб-странице. Одной из основных целей этого задания является запись вашего прогресса в изучении R. Причина, по которой я прошу вас использовать этот процесс, заключается в том, что вы можете создать веб-сайт и журнал, а также изучать R в R-studio. Это делает все действительно удобно и в том же месте.

Итак, допустим, вы изучаете, как построить гистограмму в R. Например, может быть, вы хотите выбрать 100 чисел из нормального распределения со средним значением = 0 и стандартным отклонением = 1, а затем вы хотите построить гистограмму. Вы можете сделать это прямо здесь, используя блок кода r, например:


```{r}
samples <- rnorm(100, mean=0, sd=1)
hist(samples)
```

Когда вы будете создавать этот документ R Markdown, вы увидите, что гистограмма печатается на странице вместе с кодом R. Этот документ можно настроить так, чтобы скрыть код R на веб-странице, просто удалите комментарий (хэштег) из опции cold folding  в заголовке yaml вверху. Чтобы позволить увидеть код вам и мне увидеть код, лучше оставить его таким, какой он есть. Вы узнаете обо всех этих вещах и многом другом, которые можно настроить в каждом блоке кода R.

# Идея

Используйте этот лабораторный журнал, чтобы записывать, что вы делаете в R. Так я смогу видеть, что вы делаете, и помогать вам на этом пути. Вы также будете создавать хранилище всех объектов, которые вы делаете. Вы можете делать записи-посты обо всем. Изучение конкретных задач в R (не связанных с проектом) и выполнение действий для проекта, мы обсудим в начале обучения. Вы можете начать прямо сейчас, погуглив и попробовав что-то в R. Я разместил несколько полезных стартовых ссылок на странице ссылок на этом веб-сайте.

# Что вы можете сделать прямо сейчас самостоятельно?

Трудно изучать программирование, когда у вас нет конкретных задач, которые вы пытаетесь решить. Просто все кажется абстрактным.

[Вводная книгу по программированию R] (https://crumplab.github.io/programmingforpsych/) дает некоторые [конкретные задачи, которые вам нужно решить] (https://crumplab.github.io/programmingforpsych/programming-challenges-i-learning-the-fundamentals.html).

Чтобы научиться вести журнал выполнения своих работ с помощью R и решать задачи для изучения программирования, ghtlkfuftncz использовать этот файл .Rmd для решения задач. Это будет выглядеть так:

# Задача 1

Выполните простые математические действия с числами, сложение, вычитание, умножение, деление

```{r}
1+2
2*5
5/3
(1+6+4)/5

```

# Задача 2

Поместите числа в переменные, сделайте простую математику с переменными

```{r}
a<-1
b<-2
a+b

d<-c(1,2,3)
e<-c(5,6,7)
d+e
d*e
d/e

```

# Задача 3

Напишите код, который будет помещать числа от 1 до 100 отдельно в переменную, используя цикл for. Затем, снова используя функцию seq.

```{r}
# для решения с помощью цикла for
# i становится числом от 1 до 100 на каждом шаге цикла

a <- length(100) # make empty variable, set length to 100
for (i in 1:100){
  a[i] <-i # в i присваивает число  i-му индексу a
}

print(a)

# цикл for решения #2

a<-c() #создайте пустую переменную используя комбинацию команд
for (i in 1:100){
  a<-c(a,i) # продолжает комбинировать `a` с собой и новым числом в `i`
}
print(a)

# решение с помощью 
a <- seq(1,100,1) # найдите справку для seq, используя ?seq() в консоли
print(a)

```

# Замените это на задачу 4

И продолжайте. Попробуйте решить проблемы с помощью разных скриптов, которые предлагают одно и то же решение. Удачи, счастливого кодирования.
